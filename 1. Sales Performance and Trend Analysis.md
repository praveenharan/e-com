## Appointment facts to the admission dimensions to see downstream impacts.

### Using SQL

```sql
SELECT 
    a.DiagnosisCode,
    COUNT(a.AdmissionID) AS Total_Admissions,
    ROUND(AVG(a.LengthOfStay), 1) AS Avg_Days_In_Hospital,
    MAX(a.LengthOfStay) AS Max_Stay
FROM gold_lh.dbo.dim_admissions a
GROUP BY a.DiagnosisCode
ORDER BY Avg_Days_In_Hospital DESC;

```
<img width="1422" height="602" alt="image" src="https://github.com/user-attachments/assets/f0c48876-0ee6-46c6-b080-499bc36c5f20" />
